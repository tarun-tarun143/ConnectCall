CONNECTCALL CORRECTED SOURCE PATCH
=================================

This ZIP is based on the source ZIP you uploaded.

Replace these files in your EXISTING:
F:\connect_call

The bundle contains:
- lib\main.dart
- lib\core\utils\zego_ids.dart
- lib\services\auth_service.dart
- lib\services\user_service.dart
- lib\services\permission_service.dart
- lib\services\zego_call_service.dart
- lib\screens\contacts\contacts_screen.dart
- pubspec.yaml
- android\app\src\main\AndroidManifest.xml
plus copied model/history/UI support files.

IMPORTANT:
1. KEEP your existing:
   F:\connect_call\lib\core\constants\app_config.dart
   F:\connect_call\lib\firebase_options.dart
   F:\connect_call\third_party\
   F:\connect_call\android\app\google-services.json
   Your existing Gradle/toolchain files.

2. The new code deliberately does not include your private ZEGO AppSign.

3. Your existing app_config.dart must contain the correct real ZEGO AppID/AppSign.

4. Run:
   cd /d F:\connect_call
   flutter clean
   flutter pub get
   flutter analyze
   flutter build apk --debug

5. Both accounts must be logged in at the same time for the foreground
   1-to-1 invitation test.

Main fixes:
- One shared deterministic ZEGOCLOUD ID for Firebase users.
- ZEGO initialization uses the documented IZegoUIKitPlugin type.
- Permission popup happens before an audio/video invitation.
- Calling buttons verify that the invitation service is initialized.
- Unsupported ZegoLayout/ZegoViewPosition APIs are not used.
- Firebase uses the generated firebase_options.dart already in your project.
